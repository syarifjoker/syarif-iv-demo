import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    screen: {
        alignItems: 'center',
        justifyContent: 'center',
        flex: 1,
    },
    lottieView: {
        height: 135,
        width: 135,
    },
    lottieWraper: { borderRadius: 70 },
});

export default styles;
